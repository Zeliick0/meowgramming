#ifndef LIB_H
#define LIB_H

#include <stdio.h>
#include <string.h>
#include <strings.h>
#include <stdlib.h>
#define buffer 1024

int is_digit(char number);
int total_sum(const char *filename);

#endif
